version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: crushed_dev
      POSTGRES_USER: crushed
      POSTGRES_PASSWORD: dev_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: